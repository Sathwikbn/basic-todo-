import { CapacitorConfig } from "@capacitor/core";

const config: CapacitorConfig = {
  appId: "com.advanced.todo",
  appName: "AdvancedTodo",
  webDir: "dist",
  bundledWebRuntime: false
};

export default config;
